import 'package:flutter/material.dart';

class ToPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('To Page')),
      body: Center(child: Text('This is the "To" page!')),
    );
  }
}
